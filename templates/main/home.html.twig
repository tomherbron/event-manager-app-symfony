{% extends 'base.html.twig' %}

{% block title %}ENI Event | Accueil{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <h1>Hello les ENIsiens &#128640</h1>

        <p>Bienvenue sur le site ENI Event, où vous pourrez vous inscire à tous les évènements organisés
        en dehors de l'école.</p>
        <ul>
            <li>Organisez vos propres évènements</li>
            <li>Rencontrez d'autres étudiants de l'école</li>
            <li>Have fun !</li>
        </ul>
        <div>
            <img src="{{ asset('img/nantes.jpg') }}" alt="campus-eni">
        </div>
    </div>

    {% if not app.user %}
        <a class="btn btn-outline-light rounded-0" href="{{ path('app_login') }}">Se connecter</a>
    {% else %}
        <a class="btn btn-outline-light rounded-0" href="{{ path('sortie_list') }}">Consulter les sorties</a>
        <br>
        <a class="btn btn-outline-light rounded-0" href="{{ path('sortie_add') }}">Créer une sortie</a>
    {% endif %}
{% endblock %}
